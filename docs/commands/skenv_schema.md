<!-- Generated by `make docs` from the skenv command definitions; do not edit. -->

## skenv schema

Print the JSON Schema of the skenv file or the tool config

### Synopsis

Print the JSON Schema of this skenv version to stdout: `skenv` (default) for
the skenv file (`skenv.toml`, `.yaml`, `.yml` or `.json` with `[repo]` and
`[environment]`), `config` for the tool config `~/.config/skenv/config.*`.

Files that skenv writes name their schema in a directive, so most editors
need no setup. Use this for offline work or a custom mapping, for example a
JSON Schema mapping in JetBrains IDEs or a rule in `.taplo.toml`. The same
schemas are published at https://qunaxis.github.io/skenv/schemas/.

```
skenv schema [skenv|config] [flags]
```

### Examples

```console
$ skenv schema > skenv.schema.json
$ skenv schema config
```

### Options

```
  -h, --help   help for schema
```

### SEE ALSO

* [skenv](skenv.md)	 - Keep agent skills (Claude Code, Codex, pi) in sync with a manifest

