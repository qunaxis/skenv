name: release

# Manual: computes the next version from Conventional Commits and publishes
# it (scripts/release.sh). Does nothing when there is nothing to release.
on:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          ref: main
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
      - uses: taiki-e/install-action@v2
        with:
          tool: git-cliff@2.14.2
      - name: install lefthook and gitleaks (hook acceptance test)
        run: |
          go install github.com/evilmartians/lefthook/v2@v2.1.14
          go install github.com/zricethezav/gitleaks/v8@v8.30.1
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - uses: goreleaser/goreleaser-action@v7
        with:
          version: v2.18.2
          install-only: true
      - name: make release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          make release
